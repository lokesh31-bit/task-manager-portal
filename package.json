{
  "name": "task-manager-portal",
  "version": "1.0.0",
  "description": "Mini Project Management Portal - Fresher Hiring Assessment",
  "main": "index.js",
  "scripts": {
    "setup": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "backend": "npm run start --prefix backend",
    "frontend": "npm run dev --prefix frontend",
    "dev": "concurrently \"npm run start --prefix backend\" \"npm run dev --prefix frontend\"",
    "test": "npm run test --prefix backend"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
